server:
  port: 8089

spring:
  application:
    name: java-gateway
  ai:
    openai:
      api-key: ${OPENAI_API_KEY}
      chat:
        options:
          model: gpt-4o-mini
          temperature: 0.0

python-service:
  url: ${PYTHON_SERVICE_URL:http://localhost:8099}
